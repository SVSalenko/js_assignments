let userEnteredNumber = 12;
let result;

result = 18 + userEnteredNumber;

result = result - 10;
result = result * 2;
result = result / 5;

// Значение переменно введенной поьзователем не изменилось.

alert('Результат - ' + result);
alert('Число, введённое пользователем - ' + userEnteredNumber);
